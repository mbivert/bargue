{{- $lang := index .args 0 -}}
<!DOCTYPE>
<html>
	<head>
		<meta charset="utf-8">
		<link type="text/css" rel="stylesheet" href="{{ .db.baseurl }}/style.css">
		<title>{{ index .db.title $lang }}</title>
		<meta name="description" content="{{ index .db.description $lang }}" />
		<meta name="robots" content="index, archive" />
		<meta name="keywords" content="{{ join (index .db.keywords $lang) ", "}}" />
		<meta name="author" content="{{ .db.author }}" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	</head>

	<body>
		<header>
			<nav>
				<div id="menu">
					<a class="menu-entry" href="{{ .db.baseurl }}/" >🏠</a>
					<a class="menu-entry" href="{{ .db.baseurl }}/parts-of-the-head/">
						{{- index .db.i18n "parts-of-the-head" $lang -}}
					</a>
					<div class="menu-entry">
						<a class="menu-entry-url" href="{{ .db.baseurl }}/heads/">
							{{- index .db.i18n "heads" $lang -}}
						</a>
						<div class="sub-menu">
							<a href="{{ .db.baseurl }}/heads-in-profile/">Heads in profile</a>
							<a href="{{ .db.baseurl }}/heads-in-three-quarter-views/">Heads in three-quarter views</a>
							<a href="{{ .db.baseurl }}/heads-in-frontal-views/">Heads in frontal views</a>

						</div>
					</div>
					{{- if eq $lang "fr" -}}
						<a class="menu-entry" href="{{ .db.baseurl }}/fr/a-propos/">À propos</a>
					{{- else -}}
						<a class="menu-entry" href="{{ .db.baseurl }}/about/">About</a>
					{{- end -}}
				</div>
			</nav>
			<div id="important">
				{{- if eq $lang "fr" -}}
				Documentation sur les
				<a href="https://organharvestinvestigation.net/">prélèvements d'organes</a>
				forcés en Chine.
				{{- else -}}
				You may want to <a href="https://organharvestinvestigation.net/">inform yourself</a>
				about human rights in China.
				{{- end -}}
			</div>
		</header>
